# Docker and environment
.env
.env.local
.env.*.local
!.env.template

# Docker volumes
workspace/*           # Project files (users should manage separately)
!workspace/.keep

# Neovim config (pre-configured as part of project)
# config/nvim/*         # User's Neovim configuration - now tracked in repo
# !config/nvim/.keep

# Container data
# (No Tailscale volume - Tailscale installed on host if needed)

# Build artifacts
*.tar.gz
*.zip

# Editor and IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Logs
*.log
logs/
session-ses_*.md

# Temporary files
tmp/
temp/