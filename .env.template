# Docker IDE Environment Variables
# Copy this file to .env and fill in your values
# WARNING: Never commit .env file to version control!

# ===== OPTIONAL: AI Service Credentials =====
# Anthropic API key for Claude Code
# Get from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxx

# OpenCode API key (if using OpenCode Zen)
# Get from: https://opencode.ai/auth after running `/connect` in OpenCode
OPENCODE_API_KEY=oc-xxxxxxxxxxxxxxxxxxxx

# ===== OPTIONAL: Git Configuration =====
# Git author info for commits made inside container
GIT_AUTHOR_NAME="Your Name"
GIT_AUTHOR_EMAIL="your.email@example.com"

# ===== NOTES =====
# 1. The .anthropic directory will be mounted from host (~/.anthropic)
#    If you authenticate Claude Code once, tokens will persist there
# 2. The .opencode directory will be mounted from host (~/.opencode)
#    OpenCode configuration and cache will persist there
# 3. For SSH/Git access, uncomment volume mounts in docker-compose.yml
#    and ensure proper permissions on host SSH keys
# 4. Access via SSH to host server (Tailscale installed on host):
#    ssh ubuntu@oracle-server-tailscale-ip
#    cd my-ide
#    docker exec -it ide-devbox bash